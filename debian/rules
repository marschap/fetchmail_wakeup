#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	dh_auto_build -- PLUGIN_NAME=lib90_fetchmail_wakeup_plugin.so

override_dh_auto_install:
	dh_auto_install -- PLUGIN_NAME=lib90_fetchmail_wakeup_plugin.so

override_dh_auto_clean:
	dh_auto_clean -- PLUGIN_NAME=lib90_fetchmail_wakeup_plugin.so

